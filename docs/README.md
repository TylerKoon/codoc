### Codoc
Codoc is a GitHub action that helps keep track of documentation.

#### src/myfile.ts
This file contains a function `compute` that takes in two numbers (num1 and num2), and returns the product of these two numbers.